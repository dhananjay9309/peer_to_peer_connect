name: flutter_offline_chat
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  wifi_info_plugin: ^1.1.5
  ping_discover_network: ^0.2.0+1
  flare_flutter: ^2.0.3
  intl:
  wifi_iot:
  introduction_screen: ^2.0.0
  bubble: ^1.2.1
  awesome_dialog: ^1.3.2
  image_picker: ^0.7.2+1



  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/
